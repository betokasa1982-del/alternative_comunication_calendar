{
  "name": "visual-routine",
  "private": true,
  "description": "Visual Routine — single-file PWA. `npm test` runs the jsdom test suite against index.html.",
  "scripts": { "test": "node tests/run.js" },
  "devDependencies": { "jsdom": "^24.0.0" }
}
